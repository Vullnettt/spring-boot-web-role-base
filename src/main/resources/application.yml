server:
  port: 8080
spring:
  application:
    name: spring-boot-web-role-base
  datasource:
    password: vullnet2004
    url: jdbc:postgresql://localhost:5432/rolebase
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true


